<h1>Channels</h1>

<ul>
  <% if @channels %>
  <% @channels.each do |channel|%>
    <p><%= channel.callsign %> | <%= channel.name %> | $<%= channel.price_per_month %>/mo</p>
     <% end %>
  <% end %>
</ul>
